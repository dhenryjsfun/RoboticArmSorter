{
  "name": "package-sorter",
  "version": "1.0.0",
  "description": "Robotic arm package dispatcher - sort packages by volume and mass",
  "main": "dist/sort.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
